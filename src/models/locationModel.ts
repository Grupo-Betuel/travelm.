export interface ILocation {
    address: string;
    city: string;
    province: string;
    country: string;
    latitude: string;
    longitude: string;
    link?: string;  // Assuming 'link' might be optional
}
